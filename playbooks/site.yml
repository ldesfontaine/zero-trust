# ============================================================================
# Raccourci — Tout d'un coup (après enrollment NetBird)
# Usage : ansible-playbook -i inventory.ini playbooks/site.yml --ask-vault-pass
# ============================================================================
---
- ansible.builtin.import_playbook: infrastructure.yml  # noqa: name[play]
- ansible.builtin.import_playbook: services.yml  # noqa: name[play]
- ansible.builtin.import_playbook: mesh-config.yml  # noqa: name[play]
