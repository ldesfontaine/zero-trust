# Managed by Ansible — ne pas modifier manuellement
# Secrets Bientôt serveur (Pi) — déployé mode 0600

# --- Bientôt serveur ---
BIENTOT_AGENTS=sentinelle:{{ vault_bientot_token_vps }},cerveau:{{ vault_bientot_token_pi }}
DASHBOARD_ADDR=0.0.0.0:{{ bientot_port }}
AGENT_ADDR=0.0.0.0:{{ bientot_agent_port }}
DB_PATH=/data/bientot.db
NTFY_SERVER_URL=https://{{ ntfy_subdomain }}.{{ vault_base_domain }}
NTFY_TOPIC=bientot-alerts
NTFY_TOKEN={{ vault_ntfy_auth_token }}
VEILLE_URL=http://veille-secu:{{ veille_secu_web_port }}
VEILLE_TOKEN={{ vault_veille_token }}
