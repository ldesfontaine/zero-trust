---
# ============================================================================
# Monitoring — Variables internes
# Les seuils, feature flags et intervalles sont dans group_vars/all.yml
# et host_vars/ (source de vérité unique).
# ============================================================================

# --- Ntfy (fallback si pas de variable vault) ---
ntfy_server_url: "{{ vault_ntfy_server_url | default('https://ntfy.' + vault_base_domain) }}"
ntfy_alert_topic: "{{ vault_ntfy_alert_topic | default('alerts') }}"
