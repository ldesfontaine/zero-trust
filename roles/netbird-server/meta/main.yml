---
# traefik est un prérequis mais géré par l'ordre des rôles dans le playbook.
# Ne PAS le déclarer ici : double exécution → tâches en doublon dans les logs.
dependencies: []

allow_duplicates: false
