# Managed by Ansible — ne pas modifier manuellement
# Secrets injectés via ansible-vault → fichier .env déployé mode 0600

# --- CrowdSec ---
BOUNCER_KEY_firewall={{ vault_crowdsec_lapi_key }}

# --- Ntfy ---
NTFY_AUTH_DEFAULT_ACCESS=deny-all

# --- Bientôt Agent ---
BIENTOT_SERVER_URL=https://{{ bientot_subdomain }}.{{ vault_base_domain }}
BIENTOT_PUSH_TOKEN={{ vault_bientot_push_token }}
