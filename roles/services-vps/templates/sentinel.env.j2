# Managed by Ansible — ne pas modifier manuellement
# .env Docker Compose — lu automatiquement pour l'interpolation ${}
# Chaque container a son propre env_file (ntfy.env, bientot.env)

# --- CrowdSec (interpolation Docker Compose ${BOUNCER_KEY_firewall}) ---
BOUNCER_KEY_firewall={{ vault_crowdsec_lapi_key }}
