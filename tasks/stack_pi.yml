# ============================================================================
# Stack Pi — Nextcloud, Vaultwarden, Grafana, VictoriaMetrics,
#            AdGuard Home, BentoPDF, Portfolio (termfolio)
#
# Données persistantes → /mnt/tank/stack  (ZFS mirror NVMe)
# Images Docker        → /var/lib/docker  (SD card 64Go — défaut Docker)
#
# Deux réseaux Docker isolés :
#   brain_public  — BentoPDF, Portfolio (accessibles via VPS → mesh)
#   brain_private — Nextcloud, Vaultwarden, Grafana, AdGuard (VPN admin only)
# ============================================================================
---

# --- Création de l'arborescence sur le tank ---
- name: Création des dossiers de la stack Pi sur /mnt/tank
  file:
    path: "{{ item }}"
    state: directory
    owner: "{{ vault_pi_ssh_user }}"
    group: "{{ vault_pi_ssh_user }}"
    mode: '0755'
  loop:
    - "{{ pi_stack_dir }}"
    - "{{ pi_stack_dir }}/bentopdf"
    - "{{ pi_stack_dir }}/portfolio"
    - "{{ pi_stack_dir }}/nextcloud"
    - "{{ pi_stack_dir }}/nextcloud/data"
    - "{{ pi_stack_dir }}/vaultwarden"
    - "{{ pi_stack_dir }}/grafana"
    - "{{ pi_stack_dir }}/victoriametrics"
    - "{{ pi_stack_dir }}/victoriametrics/vmagent-config"
    - "{{ pi_stack_dir }}/adguard"
    - "{{ pi_stack_dir }}/adguard/work"
    - "{{ pi_stack_dir }}/adguard/conf"
    - "{{ pi_stack_dir }}/grafana-provisioning"
    - "{{ pi_stack_dir }}/grafana-provisioning/datasources"

- name: Fix permissions Grafana (UID 472)
  file:
    path: "{{ pi_stack_dir }}/grafana"
    owner: "472"
    group: "472"
    mode: '0755'

# --- Déploiement des configs ---
- name: Déploiement du docker-compose Pi
  template:
    src: templates/docker-compose_pi_yml.j2
    dest: "{{ pi_stack_dir }}/docker-compose.yml"
    owner: "{{ vault_pi_ssh_user }}"
    mode: '0644'
  notify: Reload Docker Stack Pi

- name: Déploiement de la datasource Grafana (VictoriaMetrics)
  template:
    src: templates/grafana_provisioning_datasources_yml.j2
    dest: "{{ pi_stack_dir }}/grafana-provisioning/datasources/victoriametrics.yml"
    owner: "{{ vault_pi_ssh_user }}"
    mode: '0644'
  notify: Reload Docker Stack Pi

- name: Déploiement de la config vmagent (scrape Pi + VPS via NetBird)
  template:
    src: templates/vmagent_config_yml.j2
    dest: "{{ pi_stack_dir }}/victoriametrics/vmagent-config/vmagent-config.yml"
    owner: "{{ vault_pi_ssh_user }}"
    mode: '0644'
  notify: Reload Docker Stack Pi

# --- Config initiale AdGuard (skip le setup wizard) ---
- name: Vérifier si AdGuard est déjà configuré
  stat:
    path: "{{ pi_stack_dir }}/adguard/conf/AdGuardHome.yaml"
  register: adguard_conf_file

- name: Déployer la config initiale AdGuard (premier run uniquement)
  template:
    src: templates/adguardhome_yaml.j2
    dest: "{{ pi_stack_dir }}/adguard/conf/AdGuardHome.yaml"
    owner: "{{ vault_pi_ssh_user }}"
    mode: '0644'
  when: not adguard_conf_file.stat.exists

# Synchronise uniquement le hash du mot de passe si les variables changent.
# Ne touche pas au reste de la config (filtres, rewrites, settings UI).
- name: Synchroniser le hash du mot de passe AdGuard
  replace:
    path: "{{ pi_stack_dir }}/adguard/conf/AdGuardHome.yaml"
    regexp: '(    password: ).*'
    replace: '\1"{{ vault_adguard_admin_password_hash }}"'
  when: adguard_conf_file.stat.exists
  notify: Restart AdGuard

# --- Attendre que NetBird soit connecté (nécessaire pour le binding des ports) ---
- name: Attendre l'interface NetBird (IP {{ vault_pi_netbird_ip }})
  shell: ip addr show | grep -q "{{ vault_pi_netbird_ip }}"
  register: nb_iface_check
  retries: 30
  delay: 2
  until: nb_iface_check.rc == 0
  changed_when: false
  when: vault_pi_netbird_ip != "REMPLACER_APRES_ENROLEMENT_NETBIRD"

# --- Lancement ---
- name: Arrêt propre du container AdGuard avant relance
  shell: "docker stop adguard 2>/dev/null || true && docker rm adguard 2>/dev/null || true"
  become_user: "{{ vault_pi_ssh_user }}"
  changed_when: false

- name: Clone le dépôt du portfolio sur le Pi
  ansible.builtin.git:
    repo: "https://github.com/ldesfontaine/termfolio.git"
    dest: "{{ pi_stack_dir }}/portfolio"
    version: main
    force: yes
  become_user: "{{ vault_pi_ssh_user }}"

- name: Builder l'image Docker du portfolio (directement sur le Pi)
  ansible.builtin.command: docker build --network=host -t termfolio:local .
  args:
    chdir: "{{ pi_stack_dir }}/portfolio"
  become_user: "{{ vault_pi_ssh_user }}"


# Pull uniquement les images registry (pas les images build: locales)
# --ignore-buildable évite l'erreur "pull access denied" sur termfolio:local
- name: Pull des images registry (hors images locales)
  shell: |
    cd {{ pi_stack_dir }} && \
    docker compose pull grafana nextcloud nextcloud-db vaultwarden victoriametrics vmagent node-exporter adguard adguard-exporter bentopdf
  become_user: "{{ vault_pi_ssh_user }}"
  changed_when: false


- name: Lancement de la Stack Pi
  shell: "cd {{ pi_stack_dir }} && docker compose up -d --remove-orphans"
  become_user: "{{ vault_pi_ssh_user }}"

- name: "Import dashboards Grafana (Node Exporter, CrowdSec, AdGuard)"
  include_tasks: tasks/grafana_dashboards.yml